{
  "name": "@looplia-core/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "looplia": "./dist/index.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "test": "bun run build && vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run commands/",
    "test:e2e": "bun run build && vitest run e2e/",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@looplia-core/core": "workspace:*"
  },
  "devDependencies": {
    "@looplia-core/config": "workspace:*",
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.0.0",
    "vitest": "^2.0.0"
  }
}
